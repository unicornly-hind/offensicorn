---
// src/layouts/Layout.astro
const { title = "Offensicorn – Hind", description = "" } = Astro.props;
import "../styles/globals.css";
---

<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
<header class="hero-header">
      <nav class="hero-nav">
        <a href="/" class={Astro.url.pathname === "/" ? "active" : ""}>Accueil</a>
        <a href="/a-propos" class={Astro.url.pathname === "/a-propos" ? "active" : ""}>À propos</a>
        <a href="/art-de-dejouer" class={Astro.url.pathname === "/art-de-dejouer" ? "active" : ""}>L’art de déjouer</a>
        <a href="/blog" class={Astro.url.pathname === "/blog" ? "active" : ""}>Blog</a>
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer>
      <p>© {new Date().getFullYear()} Hind – Offensicorn 🦄</p>
    </footer>
  </body>
</html>
